PORT=5050
JWT_SECRET=84a56796d2c525e0da72e4801670fc8dcd84ab44c2b41357e30c05f1b5e39fb1ac100f8619f9a047e439bc1ab3fd4d11

# PostgreSQL connection string.
# Local example:  postgresql://postgres:postgres@localhost:5432/matchify
# Render example: use the "Internal Database URL" from your Render Postgres instance
DATABASE_URL=postgresql://mami%%%%Ji78@host:5432/matchify

# Set to true only if your Postgres provider needs SSL and its URL doesn't
# already contain "render.com" (Render's URL is detected automatically).
PGSSL=false

# AI features (bio generator, icebreakers, compatibility explanations,
# message translation) use the Anthropic API. Leave blank to run entirely on
# built-in fallback templates — every AI route still works without a key.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
